# react-api

El siguiente proyecto constituye una prueba tecnica clasica de trabajo, en la cual consiste en el consumo de datos de una API desde el frontend.
En el mismo se desarrollo de forma completa integrando componentes, reducer y context.
